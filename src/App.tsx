import { useState } from 'react';

function App() {
  const [count, setCount] = useState(0);

  return (
      <h1 className="text-2xl font-bold text-violet-500">Hello </h1>
  );
}

export default App;
